server:
  address: 0.0.0.0
  port: 9000
  error:
    whitelabel:
      enabled: false
    include-message: always
    # Esto de abajo para que no aparezca la traza de los errores
    include-stacktrace: never
